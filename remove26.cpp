// Leetcode 26. Remove Duplicates from Sorted Array

#include<iostream>
#include<vector>
using namespace std;

class Solution {
public:
    int removeDuplicates(vector<int>& nums) {
        for( vector<int>::iterator i=nums.begin(); i++; i != nums.end()){
            
            
        }
		return 0;
    }
};

int main(){
	Solution sol;
	vector<int> vec;
	vec = [1, 1, 2, 2, 4, 5];
	sol.removeDuplicates(vec);
	
	for (vector<int>::iterator i=nums.begin(); i++; i != nums.end()){
		cout << *i << endl;
	}
	
}
